* **Purpose**: Introduction to causal analysis. The code in ASS_1/ASS_1.Rmd (or .R script) imports the data (e.g., a .csv or .RData file in the folder) and provides a descriptive analysis. The characteristics of the sample are examined, and the treatment and control groups are compared.
* **Main methods**: calculation of summary statistics (means, standard deviations), tests for differences in means (t-test, Wilcoxon), and visualizations (bar charts, boxplots, histograms). The similarity between groups and the ignorability hypothesis are evaluated.
* **Key files**: The main file is, for example, ASS_1/ASS_1.Rmd (R Markdown) or a script named analysis_ASS1.R. Any data files are located in subfolders (e.g., ASS_1/data.csv).
* **Dependencies**: R packages for statistical analysis and graphics such as tidyverse, dplyr, ggplot2, stats for tests, and knitr/rmarkdown if it is an R Markdown file.
* **Execution**: Open the .Rmd file in RStudio and knit it (or in R: `rmarkdown::render(“ASS_1/ASS_1.Rmd”)`), or run `Rscript ASS_1/analysis_ASS1.R` in the R console.
* **Expected output**: an HTML/PDF report with summary tables and graphs, as well as any output files (.csv tables, .png graphs) saved locally.

